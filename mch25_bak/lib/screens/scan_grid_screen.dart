import 'package:flutter/material.dart';

class ScanGridScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "ScanGrid Screen (Placeholder)",
        style: TextStyle(color: Colors.white, fontSize: 24),
      ),
    );
  }
}